{
  "name": "velix-sdk-monorepo",
  "private": true,
  "description": "Velix SDK — monorepo pnpm workspaces",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "eslint packages/*/src --ext .ts",
    "prepare": "husky"
  },
  "keywords": ["velix", "biometrics"],
  "license": "UNLICENSED",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^8.62.0",
    "@typescript-eslint/parser": "^8.62.0",
    "eslint": "^10.5.0",
    "globals": "^17.7.0",
    "husky": "^9.1.7",
    "typescript": "^5.4.0"
  }
}
