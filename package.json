{
  "name": "@velix/sdk",
  "version": "0.1.0",
  "description": "Velix SDK for TypeScript/JavaScript",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "prepublishOnly": "npm run build && npm test",
    "release": "npm version patch && npm publish --access public"
  },
  "keywords": ["velix", "biometrics", "checkin"],
  "license": "UNLICENSED",
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
