export { useCheckin } from './hooks/useCheckin'
export { useGuest } from './hooks/useGuest'
// useEventStatus removido: GET /v1/events/:id/status não existe na API real
// (apenas os 6 endpoints /v1/api/* documentados na spec #593/#656).
