{
  "name": "@velix/sdk-node",
  "version": "0.1.0-alpha.1",
  "description": "Velix SDK — extensões server-side (webhooks HMAC, retry, streaming)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "postinstall": ""
  },
  "dependencies": {
    "@velix/sdk-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
